# 多目录挂载示例 —— 复制为本地 mounts.txt 后改成你自己的路径
# 每行一项,格式:HostPath 或 HostPath=vmSubdir
# 简写时子目录名取宿主目录最后一级
# `#` 起始为注释,空行忽略
#
# 示例:
# D:\code\my-project
# E:\work\another-repo=alias
#
# 启动:.\launch.ps1 start -NoRootWorkspace(自动读此文件,子目录挂到 ~/workspace 下)
# mounts.txt 是本地配置,已在 .gitignore 中
