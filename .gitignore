# 可写状态已移到包外状态目录(默认 %USERPROFILE%\.cc-sandbox\),包内不再产生运行时产物
# 旧布局遗留的仓库根状态文件(迁移后原样保留),避免误提交
/.ssh-key
/.ssh-key.pub
/.tunnel.pid
/.cloud-init.rendered.yaml
/workspace/
/mounts.txt
/bundle/

# 本地安装/进度测试脚本
test-progress.ps1

# Claude Code 本地权限设置(.local. 后缀,每台机器不同)
.claude/settings.local.json

.idea/
